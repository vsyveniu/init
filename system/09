 #the same shit as finger, just less detailed
  who  

#just show a usernames
#users

#displays a list of all logged in users since the file /var/log/wtmp was created. 
#last 

#this files contain all attepmpts to login with authentication reports
#/var/log/secure

#show each user currently logged
#finger  

#print user with current effective id
#whoami 

netstat -a
netstat -anp --tcp --udp
netstat -s
ss -l
iftop