#kill the last process
sudo kill $!

#jobs -l to look up the background process then  
#sudo kill 'number of process'
#or
#sudo pkill 'name of process'


find a process by command jobs [num]

kill %num must show [num] Terminated    ps